---
description: Start an ask-only selected-window session
---

Call the `start` tool with no window title or ID so the Windows picker opens. Confirm that frames are captured locally and sent for analysis only when the user asks a question about the selected view.
