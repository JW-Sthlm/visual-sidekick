---
description: Start Visual Sidekick for a window selected by the user
---

Call the `start_live` tool with no window title or ID so the Windows picker opens. Confirm that the visible companion panel is active. Explain that it responds only after meaningful visual changes and that questions entered in the panel stay in the same visual conversation.

Mention `/sidekick-region`, `/sidekick-status`, `/sidekick-pause`, `/sidekick-resume`, `/sidekick-stop`, and `/sidekick-ask`. Do not invent other commands.
