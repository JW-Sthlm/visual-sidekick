---
description: Compare the previous meaningful keyframe with the current view
---

Call `capture_changes`. Explain only the meaningful visible differences between the previous keyframe and the current view. If no earlier keyframe exists, say that another meaningful change is needed first.
